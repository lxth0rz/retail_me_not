{
    "title": "Inputs",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "CategoriesOnly": {
            "title": "Categories Only",
            "type": "boolean",
            "description": "Scrape only the categories names and URLs from RetailMeNot.",
            "default": false
        },
        "proxyConfig": {
            "title": "Proxy",
            "type": "object",
            "description": "Proxy servers let you bypass website protections, avoid IP address blocking and view content for other countries. Try to use the proxy if you are experiencing timeout errors.",
            "default": {},
            "editor": "proxy"
        }
    },
    "required": ["CategoriesOnly"]
}
